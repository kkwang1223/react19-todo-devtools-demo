# ⚛️ React DevTools 완전 정복

> 💡 알고 있다고 생각했지만, 몰랐던 기능들

---

## 📋 목차

| # | 섹션 | 핵심 키워드 |
|---|------|------------|
| 01 | DevTools 개요 & 설치 | 버전, 설정, 환경 |
| 02 | Components 패널 심화 | 트리 탐색, props/state 편집 |
| 03 | Profiler 완전 분석 | Flamegraph, Ranked Chart, Timeline |
| 04 | 숨겨진 기능 & 꿀팁 | $r, highlight, useDebugValue |
| 05 | 실전 디버깅 패턴 | 리렌더 탐지, 메모이제이션 검증 |

---

## 01 · DevTools 개요 & 설치

React DevTools는 브라우저 개발자 도구에 **Components**와 **Profiler** 탭을 추가해주는 확장 프로그램입니다. 컴포넌트 구조를 탐색하고, props/state를 직접 수정하고, 렌더 성능을 측정할 수 있습니다.

![React DevTools extension](https://react.dev/images/docs/react-devtools-extension.png)
*크롬 DevTools에 Components, Profiler 탭이 추가된 모습*

### 버전별 주요 변경사항

| 버전 | 주요 변경 |
|------|----------|
| **v4.x** | React 16.8+ Hook 지원, Suspense/Concurrent 시각화 추가 |
| **v5.x** | Timeline 패널 추가, 컴포넌트 필터 개선 |
| **v6.x** | React 18 Transitions 지원, Profiler 개선 |

### 설정 체크리스트

> ✅ **프로덕션 빌드**에서는 DevTools가 자동으로 비활성화되는지 확인
> (`process.env.NODE_ENV === 'production'` 일 때 hook이 제거됨)

> ✅ **크롬 확장 vs 독립 실행형 앱** — 상황에 맞게 선택하기
> - 일반 웹 개발 → 크롬 확장 프로그램
> - React Native, Electron, iframe 환경 → `react-devtools` npm 패키지 (독립 실행형)

> ✅ **다중 React 인스턴스** 환경 주의
> 마이크로프론트엔드처럼 앱 안에 React가 여러 개 있으면 DevTools가 하나만 잡을 수 있어요. 이때는 독립 실행형 앱 사용을 권장합니다.

![React DevTools standalone](https://react.dev/images/docs/react-devtools-standalone.png)
*독립 실행형 DevTools — 브라우저 확장 없이 별도 창으로 사용*

---

## 02 · Components 패널 심화

Components 패널은 React 컴포넌트 트리를 탐색하고, props/state를 실시간으로 확인·수정할 수 있는 패널입니다.

### 🔍 트리 탐색 & 검색

| 기능 | 방법 |
|------|------|
| 이름으로 검색 | 검색창에 컴포넌트명 입력 |
| **정규식 검색** | 검색창 우측 `.*` 아이콘 활성화 → `/Button$/` 같은 패턴 입력 |
| 브레드크럼 이동 | 패널 상단 경로를 클릭하면 부모 컴포넌트로 빠르게 이동 |
| DOM 컴포넌트 숨기기 | Settings → Components → "Hide components where..." 설정 |

### ✏️ Props & State 실시간 편집

컴포넌트를 선택하면 오른쪽 패널에서 props, state, hooks 값을 **직접 클릭해서 수정**할 수 있습니다. 함수형 컴포넌트의 useState 값도 편집 가능하고, 수정한 결과가 화면에 즉시 반영됩니다.

![Viewing props and state](https://legacy.reactjs.org/1f4d023f1a0f281386625f28df87c78f/props-and-state.gif)
*컴포넌트 선택 시 해당 commit의 props/state 확인 및 편집*

![See which props changed](https://legacy.reactjs.org/cc2a8b37bbce52c49a11c2f8e55dccbc/see-which-props-changed.gif)
*commit 간에 어떤 값이 변경됐는지 강조 표시*

### 🎛️ 고급 필터링

> ⚙️ Settings → Components 탭에서 설정

- **DOM 컴포넌트 숨기기** — `div`, `span` 같은 HTML 태그를 필터링해서 React 컴포넌트만 보기
- **라이브러리 컴포넌트 제외** — 이름 패턴으로 원하지 않는 컴포넌트 숨기기
- 필터 설정은 **localStorage에 저장**되므로 팀원과 공유가 안 됩니다. 신규 합류 시 따로 안내가 필요해요.

> 💡 **displayName 설정이 중요한 이유**
> HOC나 forwardRef를 사용하면 DevTools 트리에 `Anonymous`가 가득 차서 어떤 컴포넌트인지 알기 어렵습니다.
> ```js
> // HOC에 이름 붙이기
> const withAuth = (Component) => {
>   const Wrapped = (props) => <Component {...props} />;
>   Wrapped.displayName = `withAuth(${Component.displayName || Component.name})`;
>   return Wrapped;
> };
>
> // forwardRef에 이름 붙이기
> const Input = React.forwardRef((props, ref) => <input ref={ref} {...props} />);
> Input.displayName = 'Input';
> ```

---

## 03 · Profiler 완전 분석

Profiler는 각 컴포넌트가 렌더링되는 데 얼마나 걸리는지 기록하고 시각화해주는 패널입니다.

![Profiler tab](https://legacy.reactjs.org/static/4da6b55fc3c98de04c261cd902c14dc3/1e088/devtools-profiler-tab.png)
*Profiler 탭 — 녹화(Record) 버튼을 눌러 시작*

### Flamegraph 읽는 법

Flamegraph는 한 번의 렌더(commit)에서 어떤 컴포넌트가 얼마나 시간을 썼는지 보여줍니다.

![Flame chart example](https://legacy.reactjs.org/static/3046f500b9bfc052bde8b7b3b3cfc243/1e088/flame-chart.png)
*막대의 너비 = 렌더 시간. 넓을수록 오래 걸린 것*

- **막대 너비** = 렌더 시간 (넓을수록 느림)
- 막대를 클릭하면 해당 컴포넌트로 확대 가능

![Zoom in and out](https://legacy.reactjs.org/39ba82394205242af7c37ccb3a631f4d/zoom-in-and-out.gif)
*클릭으로 특정 컴포넌트 확대/축소*

#### 색상 해석

| 색상 | 의미 |
|------|------|
| 🟢 초록 | 빠름 — 렌더가 짧거나 렌더 없음 |
| 🟡 노랑/주황 | 보통 — 살펴볼 필요 있음 |
| 🔴 빨강 | 느림 — 최적화 대상 |
| ⬜ 회색 | 이번에 렌더링 안 됨 (좋은 것!) |

### Commit 바 차트

화면 상단의 작은 막대 차트에서 각 commit을 선택할 수 있습니다. 특별히 긴 막대가 있으면 그것부터 확인하세요.

![Commit selector bar chart](https://legacy.reactjs.org/static/bd72dec045515d59be51c944e902d263/d8f62/commit-selector.png)
*상단 막대 차트 — 각 막대 = 하나의 commit (렌더 발생)*

![Filtering commits by time](https://legacy.reactjs.org/683b9d860ef722e1505e5e629df7ef7e/filtering-commits.gif)
*시간 기준으로 짧은 commit 필터링 가능*

### Ranked Chart

렌더 시간이 긴 컴포넌트 순서대로 정렬해서 보여줍니다. 어디를 먼저 최적화할지 파악하는 데 가장 유용합니다.

![Ranked chart](https://legacy.reactjs.org/static/0c81347535e28c9cdef0e94fab887b89/1e088/ranked-chart.png)
*렌더 시간 내림차순 정렬 — 위에 있을수록 느린 컴포넌트*

### 특정 컴포넌트의 렌더 이력 보기

컴포넌트 우클릭 → "Show renders for this component"를 선택하면 해당 컴포넌트가 얼마나 자주 렌더됐는지 이력을 볼 수 있습니다.

![See all commits for a fiber](https://legacy.reactjs.org/99cb4321ded8eb0c21ae5fc673878563/see-all-commits-for-a-fiber.gif)
*특정 컴포넌트의 전체 렌더 이력 확인*

> ⚙️ **꼭 켜야 할 설정**
> Settings → Profiler → **"Record why each component rendered while profiling"** 활성화
> 이게 꺼져 있으면 "Why did this render?" 정보가 나오지 않습니다.

---

## 04 · 숨겨진 기능 & 꿀팁

### 💻 `$r` — 콘솔에서 컴포넌트 직접 접근

Components 탭에서 컴포넌트를 선택한 상태로 **콘솔에서 `$r`을 입력**하면 그 컴포넌트 인스턴스에 바로 접근할 수 있습니다. Elements 탭의 `$0`(DOM 노드)과 같은 개념입니다.

```js
// 콘솔에서 바로 확인 가능
$r.props             // 현재 props 확인
$r.state             // 클래스 컴포넌트의 state 확인
$r._debugHookTypes   // 훅 종류 확인
```

> 상태를 직접 바꿔보거나, props 값을 검증할 때 아주 빠르게 활용할 수 있습니다.

---

### 🎨 Highlight Updates — 리렌더 시각화

**Settings → General → "Highlight updates when components render"** 를 켜면, 컴포넌트가 렌더될 때마다 화면에 색으로 표시됩니다.

- 색이 **파란 → 초록 → 노랑 → 주황 → 빨강** 순으로 강해질수록 렌더 빈도가 높은 것
- 인터랙션을 해보면서 예상치 못한 컴포넌트가 빨개지면 불필요한 리렌더가 있다는 신호

> 성능 문제를 찾을 때 가장 먼저 켜는 기능입니다. Profiler를 쓰기 전에 이걸로 큰 그림을 먼저 파악하세요.

---

### 🏷️ `useDebugValue` — 커스텀 훅에 레이블 달기

커스텀 훅을 DevTools에서 볼 때 값이 뭔지 알아보기 어려운 경우가 많습니다. `useDebugValue`를 쓰면 DevTools에 표시될 레이블을 직접 지정할 수 있습니다.

```js
function useAuth() {
  const [user, setUser] = useState(null);

  // DevTools에서 "Auth: logged-in (admin)" 처럼 표시됨
  useDebugValue(user, (u) => u ? `logged-in (${u.role})` : 'anonymous');

  return { user, setUser };
}
```

> 두 번째 인자(포맷 함수)는 DevTools가 열려 있을 때만 실행되므로 성능에 영향 없습니다.

---

### 🧪 Suspense & Error Boundary 강제 테스트

실제 에러나 로딩 상태를 만들지 않아도 fallback UI를 미리 확인할 수 있습니다.

| 기능 | 방법 |
|------|------|
| Suspense fallback 강제 | 컴포넌트 우클릭 → **"Suspend the selected component"** |
| Error Boundary 강제 | 컴포넌트 우클릭 → **"Trigger error boundary"** |

> QA나 디자인 검토 때 로딩 화면이나 에러 화면을 바로 보여줄 수 있어서 유용합니다.

---

### 📊 `<Profiler>` 컴포넌트 — 코드로 성능 측정

DevTools 없이 코드 레벨에서 렌더 시간을 수집할 수 있습니다. 특정 컴포넌트의 성능을 모니터링하거나, 느린 렌더를 로그로 남기고 싶을 때 씁니다.

```jsx
import { Profiler } from 'react';

function onRender(id, phase, actualDuration, baseDuration) {
  // actualDuration: 실제 렌더 시간 (ms)
  // baseDuration: 최적화 없을 때 예상 시간 (ms)
  // phase: "mount" | "update"

  if (actualDuration > 16) {
    console.warn(`[느린 렌더] ${id}: ${actualDuration}ms (${phase})`);
  }
}

<Profiler id="ProductList" onRender={onRender}>
  <ProductList />
</Profiler>
```

> `actualDuration`이 `baseDuration`보다 훨씬 낮으면 메모이제이션이 잘 되고 있다는 뜻입니다.

---

## 05 · 실전 디버깅 패턴

### 🎯 불필요한 리렌더 찾기 — 단계별 플로우

```
STEP 1  Highlight updates 켜기 → 빨갛게 물드는 컴포넌트 확인
   ↓
STEP 2  Profiler 녹화 시작 → 문제 재현 → 녹화 중지
   ↓
STEP 3  Ranked Chart에서 렌더 시간 상위 컴포넌트 클릭
   ↓
STEP 4  우측 패널 "Why did this render?" 확인
        (어떤 props/state/hook이 바뀌었는지 나옴)
   ↓
STEP 5  원인에 따라 React.memo / useMemo / useCallback 적용
   ↓
STEP 6  다시 Profiler 녹화해서 개선 효과 수치로 확인
```

### 패턴별 대응법

> **메모이제이션이 잘 됐는지 확인하는 법**
> `React.memo`를 적용했다면 Profiler에서 해당 컴포넌트가 **회색(렌더 없음)** 으로 표시되어야 합니다. 여전히 렌더된다면 "Why did this render?"에서 어떤 props가 바뀌는지 확인하세요. 객체/함수를 매번 새로 만들어서 참조가 달라지는 경우가 흔합니다.

---

> **Context 때문에 리렌더가 많다면**
> Context consumer가 불필요하게 리렌더될 때는 세 가지를 시도해볼 수 있습니다.
> 1. 자주 바뀌는 값과 잘 안 바뀌는 값으로 **Context 분리**
> 2. consumer에서 `useMemo`로 필요한 값만 파생
> 3. `use-context-selector` 같은 라이브러리 검토

---

> **displayName을 팀 컨벤션으로 만들기**
> HOC, `forwardRef`, `memo` 사용 시 displayName을 항상 명시하는 규칙을 만들면 좋습니다.
> ESLint 룰 `react/display-name`을 켜두면 빠뜨렸을 때 바로 경고가 뜹니다.

---

> **`<Profiler>`로 CI에서 성능 회귀 잡기**
> 렌더 시간이 일정 threshold를 넘으면 테스트를 실패시키도록 구성할 수 있습니다.
> ```js
> if (actualDuration > THRESHOLD_MS) {
>   throw new Error(`${id} 렌더 시간 초과: ${actualDuration}ms (허용: ${THRESHOLD_MS}ms)`);
> }
> ```

---

## ✅ Key Takeaways

| # | 핵심 | 액션 아이템 |
|---|------|------------|
| 🔍 | **displayName 설정** | HOC/forwardRef/memo 모든 곳에 명시, ESLint 룰 추가 |
| 🎨 | **Highlight updates** | 성능 문제 탐색의 첫 번째 단계로 습관화 |
| ⚡ | **Profiler 루틴화** | 기능 개발 후 Ranked Chart 확인을 PR 체크리스트에 포함 |
| 🐛 | **`$r` 활용** | 콘솔에서 컴포넌트 직접 조작 → 빠른 재현 & 검증 |
| 🎯 | **"Why did this render?"** | 불필요 리렌더 원인 파악의 시작점 |

---

> 💬 **"DevTools는 '쓰는 것'이 아니라 '습관으로 만드는 것'이 핵심입니다."**

---

### 📎 참고 링크

- [공식 문서 — React Developer Tools](https://react.dev/learn/react-developer-tools)
- [Profiler 상세 설명 블로그 포스트](https://legacy.reactjs.org/blog/2018/09/10/introducing-the-react-profiler.html)
- [Chrome 확장 설치](https://chrome.google.com/webstore/detail/react-developer-tools/fmkadmapgofadopljbjfkapdkoienihi)
